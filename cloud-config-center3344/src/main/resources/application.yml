server:
  port: 3344


eureka:
  client: #客户端注册进eureka服务类表内
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka
spring:
  application:
    name: cloud-config-center
  cloud:
    config:
      server:
        git:
          uri: https://github.com/kjqJava/springcloud-config.git
          search-paths:
            - springcloud-config
      label: master
